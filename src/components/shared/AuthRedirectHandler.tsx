"use client";

/**
 * Legacy component - no longer needed after removing redirect auth
 * Kept for backwards compatibility
 */
export default function AuthRedirectHandler() {
  // This component doesn't render anything
  return null;
}
